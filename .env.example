# Required — set these in Railway's Variables tab
DATABASE_URL=postgresql://user:password@host:5432/dbname
NODE_ENV=production

# Set to your live domain — controls canonical URLs, OG tags, and schema markup
BASE_URL=https://www.ihpgreenville.com

# Optional — set by Railway automatically
# PORT=3000
